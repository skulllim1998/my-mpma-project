class Category {
  constructor(id, title, color, subCategories) {
    this.id = id;
    this.title = title;
    this.color = color;
    this.subCategories = subCategories;
  }
}

export default Category;
